# ResNet18 口罩识别实验报告（2026-08-08）

> 任务：二分类——识别图片中的人是否**戴口罩**（戴口罩 / 不带口罩）
> 环境：Kaggle 免费 GPU（Tesla T4 ×2，DataParallel）+ PyTorch 2.10 + AMP 混合精度

---

## 一、实验目的

用 ResNet18 在口罩数据集上完成二分类，验证卷积神经网络在 Kaggle 免费 GPU 上的训练流程（GitHub 中转代码 → Kaggle GPU 训练 → 本地评测）。

## 二、数据集

| 集合 | 内容 | 规模 |
|---|---|---|
| 训练集 | `data/train`（mask 366 + no_mask 477） | 843 张 |
| 测试集 | `data/test`（mask / no_mask） | 103 张 |
| 单图示例 | `no_mask.jfif`（无口罩人脸） | 1 张 |

- 图片统一 Resize 至 224×224，按数据集自身统计量归一化
- 训练/验证 8:2 随机划分（random_split）

## 三、模型与训练配置

**模型**：ResNet18（baseline 版，无注意力模块）
- 残差结构：7×7 卷积下采样 + 4 个残差阶段（64→512）+ 全局平均池化 + 全连接（512→2）

**训练配置**：

| 项 | 值 |
|---|---|
| 优化器 | Adam，lr = 0.001 |
| 损失函数 | CrossEntropyLoss |
| batch size | 32 |
| epochs | 50 |
| 数据加载 | num_workers=4 |
| 并行 | 2 卡 DataParallel |
| 加速 | AMP 混合精度（FP16 前向）+ cudnn.benchmark |
| 权重保存 | 最佳验证准确率对应权重 → `best_model_baseline.pth` |

## 四、训练过程

- 训练总耗时约 **8 分钟**（50 epochs）
- 收敛曲线：loss 从 0.66 快速下降至 0.04，train acc 达 98.5%

关键节点：

| Epoch | Train Acc | Val Acc |
|---|---|---|
| 1 | 61.7% | 63.8% |
| 48 | 98.3% | 89.2% |
| 49 | 98.5% | 90.3% |

## 五、实验结果

| 指标 | 数值 |
|---|---|
| **最佳验证准确率** | **92.43%** |
| **测试集准确率** | **94.17%** |
| 单图预测（no_mask.jfif） | 不带口罩 ✓ |

**测试集错误分析**：少量"戴口罩"样本被误判为"不带口罩"，主要集中于佩戴方式不规范（口罩位置偏低/遮挡不完整）的样本，属合理边界误差。

## 六、结论

1. ResNet18 在口罩二分类任务上收敛迅速，**测试集准确率 94.17%**，模型有效；
2. Kaggle 免费 T4 + AMP 混合精度下，50 epochs 仅 8 分钟，验证了"GitHub 中转 + Kaggle GPU"流程的可行性；
3. 后续可尝试 SE / CBAM 注意力模块或 ImageNet 预训练权重（`--model se / cbam / cbam_pt`）提升精度。

## 七、产物

| 文件 | 说明 |
|---|---|
| `best_model_baseline.pth` | 最佳验证权重（42MB） |
| `train_curve_baseline.png` | 训练/验证 loss 与 acc 曲线 |

评测命令：`python -u model_test.py --model baseline`