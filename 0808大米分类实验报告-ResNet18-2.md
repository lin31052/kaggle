# ResNet18 大米分类实验报告（2026-08-08）

> 任务：5 类大米图像分类（Arborio / Basmati / Ipsala / Jasmine / Karacadag）
> 环境：Kaggle 免费 GPU（Tesla T4 ×2，DataParallel）+ PyTorch 2.10 + AMP 混合精度

---

## 一、实验目的

用 ResNet18 在大米图像数据集上完成 5 分类，验证卷积神经网络在 Kaggle 免费 GPU 上的训练流程与效果。

## 二、数据集

| 集合 | 规模 |
|---|---|
| `data/train`（5 类） | 5,485 张 |
| `data/test`（5 类） | 2,396 张 |

- 图片统一 Resize 至 224×224，按数据集自身统计量归一化
- 训练/验证 8:2 随机划分（random_split）：训练 4,388 张 + 验证 1,097 张

## 三、模型与训练配置

**模型**：ResNet18（5 类输出）
- 残差结构：7×7 卷积下采样 + 4 个残差阶段（64→512）+ 全局平均池化 + 全连接（512→5）

**训练配置**：

| 项 | 值 |
|---|---|
| 优化器 | Adam，lr = 0.001 |
| 损失函数 | CrossEntropyLoss |
| batch size | 32 |
| epochs | 50 |
| 数据加载 | num_workers=4 |
| 并行 | 2 卡 DataParallel |
| 加速 | AMP 混合精度（FP16 前向）+ cudnn.benchmark |
| 权重保存 | 最佳验证准确率对应权重 → `best_model.pth` |

## 四、训练过程

- 训练总耗时约 **39 分钟**（50 epochs，与口罩实验并行运行、CPU 存在竞争，单跑会更快）
- 收敛迅速：首个 epoch 训练准确率即达 92.99%，验证 79.74%

关键节点：

| Epoch | Train Acc | Val Acc |
|---|---|---|
| 0 | 92.99% | 79.74% |
| 2 | 98.37% | 92.91% |
| 49 | 99.91% | 99.54% |

## 五、实验结果

| 指标 | 数值 |
|---|---|
| **最佳验证准确率** | **99.71%** |
| 最终 epoch train acc | 99.91% |
| 最终 epoch val acc | 99.54% |
| 最终 epoch train/val loss | 0.0031 / 0.0164 |
| 测试集准确率 | 待评测（`python -u model_test.py`） |

训练全程无过拟合迹象（train 与 val 差距始终 < 1%），loss 平稳下降至 0.003。

## 六、结论

1. ResNet18 在大米 5 分类上收敛迅速、效果优秀，**最佳验证准确率 99.71%**；
2. AMP 混合精度 + cudnn.benchmark 在 T4 上显著提速，50 epochs 约 39 分钟完成；
3. 后续可尝试更深的 ResNet 变体或数据增强，进一步逼近 100%。

## 七、产物

| 文件 | 说明 |
|---|---|
| `best_model.pth` | 最佳验证权重（42MB） |
| `train_curve_20260808.png` | 训练/验证 loss 与 acc 曲线 |
| `train_20260808.log` | 完整训练日志 |

评测命令：`python -u model_test.py`（加载 `best_model.pth` + `data/test`）